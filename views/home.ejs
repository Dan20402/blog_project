<%- include('partials/header.ejs'); -%>
<body>
  <div class="container">
    <div class="jumbotron">
      <h1 id="jumboTitle">
        <span>Título do... </span><span id="titleSpan">Blog</span>
      </h1>
    </div>
    <div class="btn-group" role="group">
      <a href="/compose" class="btn btn-primary m-2 rounded">Nova entrada</a>
      <form action="/?_method=DELETE" method="POST"><%-mostreButton-%></form>
    </div>

    <% entrada.forEach((entrada)=>{ %>

    <div class="card p-4 mb-1 col-6">
      <h3 class="card-subtitle text-muted mb-4">
        <%=entrada.criadaEm.toLocaleDateString()%>
      </h3>
      <h1 class="card-title mb-2"><%=entrada.titulo%></h1>

      <p class="card-text">
        <%= entrada.conteudo.substring(0,100)%>
        <a href="/entry<%= entrada._id%>">...Leia mais</a>
      </p>
    </div>

    <% })%> <%- include('partials/footer.ejs'); %>
  </div>
</body>
